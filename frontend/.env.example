# 环境变量示例（复制为 .env.local 或 .env.development 后填写实际值）

# 后端 API 地址（HTTP）
# 开发环境示例：http://localhost:8080 或 http://192.168.0.130:8080（局域网访问）
NEXT_PUBLIC_API_URL=http://localhost:8080

# WebSocket 地址（可选，不设置时自动从 API 地址推导）
# NEXT_PUBLIC_WS_URL=ws://localhost:8080/ws