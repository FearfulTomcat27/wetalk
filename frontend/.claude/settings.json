{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */frontend/*.ts|*/frontend/*.tsx|*/frontend/*.js|*/frontend/*.jsx) cd \"$CLAUDE_PROJECT_DIR\"/frontend && npx eslint --fix \"$f\" 2>/dev/null ;; esac } || true"
          }
        ]
      }
    ]
  }
}
