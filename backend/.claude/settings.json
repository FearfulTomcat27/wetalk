{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */backend/*.go) GOBIN=\"$HOME/go/bin\"; \"$GOBIN/gofumpt\" -w \"$f\" && \"$GOBIN/goimports\" -w \"$f\" ;; esac } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
